# 阶段三每日任务清单：Transformer 与 GPT（第 25-36 周）

> 主线：Karpathy Zero to Hero 第 7 讲（手写 GPT）、第 8 讲（GPT 分词器），加 nanoGPT 实战与原始论文。
> 环境：`conda activate llm_env && cd ~/code/llm_learn`
> 直觉补充（可选）：3Blue1Brown《深度学习》Transformer/注意力机制那几集、Karpathy《Intro to Large Language Models》(1h)。
> 时间预算：每天约 30 分钟，每周 6 个学习日 + 周日缓冲/复盘的机制不变。原"每天 1~2 小时"版本中的 1 天任务，在本版本中拆为 3 个 30 分钟 session（看视频部分拆 1~2 个，练习部分拆 1~2 个）；原 1 周内容对应本版本的连续 3 周。

## 第 25 周：注意力机制（上）—— 数学直觉与单头 self-attention

**本周目标**：推出 scaled dot-product attention 的公式与形状流，并手写单头 causal self-attention。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 第 7 讲 GPT 视频开头前 20 分钟（attention 的数学直觉前半） | 复习 softmax 与矩阵乘法，标出 B、T、C 各维含义 | 能说出 Q、K、V 各自的张量形状 |
| 周二 | 视频开头后 20 分钟（数学直觉后半） | 纸上推 `softmax(QK^T/√d)·V` 的矩阵形状变化（B,T,C 各维度标注） | 不看资料能写出 attention 公式并说出 √d 的作用 |
| 周三 | 视频前 1/3 的前半（单头注意力代码讲解） | 在 `code/week9_attention.py` 搭单头 self-attention 骨架（QKV 线性投影） | 前向能跑通、输出形状正确 |
| 周四 | 视频前 1/3 的后半 | 实现 scaled dot-product 打分与 softmax 加权求和 | 用随机张量验证输出形状 |
| 周五 | 重看本周没懂的视频片段 / 缓冲 | 加因果掩码（上三角置 -inf） | 掩码生效：改第 t 个位置之后的输入不影响位置 t 的输出 |
| 周六 | 无新课 | 本周小结写入 `notes/`，欠账补齐 | — |
| 周日 | 休整 / 补进度 | — | — |

## 第 26 周：注意力机制（中）—— 多头、Block 与完整字符级 GPT

**本周目标**：把单头扩成多头，组装 Block 和完整 GPT，并在莎士比亚文本上跑通训练。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 第 7 讲视频中段前 20 分钟（多头注意力） | 把上周的单头扩展为 `MultiHeadAttention`（head 拆分与拼接） | 多头输出形状正确 |
| 周二 | 视频中段后 20 分钟（FFN、残差、LayerNorm 组装 Block） | 手写 `Block`（MHA + FFN + 残差 + LayerNorm） | 能解释多头相对单头在参数量和表达上的区别 |
| 周三 | 视频后段前 1/3（完整 GPT 组装） | 组装 embedding + Block×N + 线性输出头 | 模型能前向出 logits |
| 周四 | 视频后段中 1/3（训练循环） | 准备莎士比亚文本数据，先小步数跑通训练（MPS，分钟级） | loss 正常下降不 NaN |
| 周五 | 视频后段后 1/3（采样） | 训练几轮并采样 | 采样输出从乱码变成像英语的字符流 |
| 周六 | 无新课 | 本周小结写入 `notes/`，欠账补齐 | — |
| 周日 | 休整 / 补进度 | — | — |

## 第 27 周：注意力机制（下）—— 消融实验与周验收

**本周目标**：用消融实验理解残差与 LayerNorm 位置的作用，并默写通过 attention 周验收。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 选读：3b1b 注意力机制一集前半 | 设计消融实验方案：去掉残差连接、Pre-LN vs Post-LN | 方案写入 `notes/` |
| 周二 | 3b1b 后半 | 跑第一组消融：去掉残差连接，对比训练稳定性 | 记录 loss 曲线差异 |
| 周三 | 无新课 | 跑第二组消融：Pre-LN vs Post-LN，对比训练稳定性 | 记录现象到 `notes/`，能复述 Pre-LN 更稳的说法 |
| 周四 | 复习：回看本周与上周代码、笔记 | 列出还讲不清的点，重看对应视频片段 | 疑点清零或有明确结论 |
| 周五 | 复习 / 缓冲 | 默写演练：凭记忆写 attention 公式与因果掩码，对不出的地方回看代码 | 卡壳点全部弄懂 |
| 周六 | 无新课 | **周验收**：默写 scaled dot-product attention 完整公式与因果掩码实现，限时 30 分钟 | 独立写出 |
| 周日 | 休整 / 补进度 | — | — |

## 第 28 周：nanoGPT 实战（上）—— model.py 与 train.py 前半

**本周目标**：把你手写的 GPT 和 Karpathy 的工程版（`nanoGPT/model.py`，约 300 行）逐行对应，理解教学版省略了什么；读通数据管线。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 通读 `nanoGPT/model.py` 前半（配置、Block 定义） | 画对照表：你的 GPT vs nanoGPT，先填 weight tying、参数初始化两项 | 两项差异能写出动机 |
| 周二 | 通读 `model.py` 后半（weight decay 分组、flash attention、from_pretrained） | 对照表补全：weight decay 分组、flash attention | 能说出每一处差异的动机 |
| 周三 | 读 `nanoGPT/train.py` 前 1/4（数据管线） | 跑通 `python data/shakespeare_char/prepare.py`，查看生成的 bin 文件 | 能说出 bin 里存的是什么 |
| 周四 | 读 `train.py` 前 1/2 剩余部分（训练循环开头） | 读 memmap 取 batch 的代码，理解 bin 文件格式 | 能解释 memmap 为什么比读文本快 |
| 周五 | 重读没懂的段落 / 缓冲 | 把对照表和 memmap 笔记整理进 `notes/` | — |
| 周六 | 无新课 | 本周小结写入 `notes/`，欠账补齐 | — |
| 周日 | 休整 / 补进度 | — | — |

## 第 29 周：nanoGPT 实战（中）—— 本地训练与超参实验

**本周目标**：读通训练循环后半，在本地把莎士比亚字符级模型训出来，并做一轮超参消融。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 读 `train.py` 后 1/2 前半（lr schedule、梯度累积） | 按机器改配置：`batch_size` 降到 16-32、`max_iters` 降到 2000-3000（见下方提示） | 配置改好、能解释每项为什么改 |
| 周二 | 读 `train.py` 后 1/2 后半（AMP、checkpoint） | 启动本地训练：`python train.py config/train_shakespeare_char.py` | 训练正常跑起来 |
| 周三 | 无新课 | 观察训练曲线并采样 | loss 从 ~4.2 降到 2.x，采样出像莎剧的文本 |
| 周四 | 实验日：先记录基线配置与最终 loss | n_layer 翻倍或减半跑一轮，观察 loss 和显存/内存变化 | 记录对比数据 |
| 周五 | 无新课 | n_head / n_embd 翻倍或减半跑一轮 | 记录对比数据 |
| 周六 | 无新课 | 整理"参数翻倍 → loss 降多少、慢多少"，存 `notes/` | 有完整对照记录 |
| 周日 | 休整 / 补进度 | — | — |

> 提示：M4 16GB 跑 `train_shakespeare_char.py` 时把 `batch_size` 降到 16-32、`max_iters` 降到 2000-3000 即可；报 OOM 就再减半并 `torch.mps.empty_cache()`。

## 第 30 周：nanoGPT 实战（下）—— 预训练选读与周验收

**本周目标**：理解"架构相同 → 权重可互换"，并默画 GPT 前向传播的完整张量形状流。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 选读：Karpathy《Deep Dive into LLMs like ChatGPT》(3.5h) 前 1/3 的前半（预训练数据部分） | 用 `transformers` 加载 GPT-2 权重，打印各层形状 | 权重加载成功 |
| 周二 | 选读：前 1/3 的后半（tokenizer 部分） | 对比 nanoGPT 的 `from_pretrained` 加载路径与 HF 权重 | 理解"架构相同 → 权重可互换" |
| 周三 | 无新课 | 把权重对应关系整理进 `notes/` | 能讲清权重张量如何一一对应 |
| 周四 | 复习：把 `model.py` 前向传播再读一遍 | 自己画一遍从 token ids 到 logits 的张量形状流 | 形状流草图完成 |
| 周五 | 复习 / 缓冲 | 合上代码默画形状流，对不出的地方重读 `model.py` | 卡壳点全部弄懂 |
| 周六 | 无新课 | **周验收**：不查代码说出 GPT 一次前向传播的完整张量形状流（从 token ids 到 logits） | 讲不出就重读 `model.py` |
| 周日 | 休整 / 补进度 | — | — |

## 第 31 周：分词器 —— 手写 BPE 与 tiktoken 对比

**本周目标**：理解 tokenization 是"LLM 的编解码层"，手写最简 BPE 并用 tiktoken 做对比实验。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 第 8 讲 GPT Tokenizer 视频前 1/4（Unicode、UTF-8） | 读 `minbpe/` 代码结构，写出 pair 统计函数 | 能正确统计相邻 pair 频次 |
| 周二 | 视频前 1/2 后半（BPE 算法） | 在 `minbpe/` 基础上完成合并与迭代，写出最简 BPE | BPE 训练循环跑通 |
| 周三 | 无新课 | 对一段英文训练出词表并测试 | encode/decode 往返一致 |
| 周四 | 视频后 1/2 前半（特殊 token） | 用 `tiktoken` 以 GPT-2 编码同一句话，数 token | 编码成功、记录 token 数 |
| 周五 | 视频后 1/2 后半（GPT-2/GPT-4 词表差异） | 同一句换 cl100k 编码，对比 token 数 | 能解释为什么中文 token 效率低、代码为什么要单独处理 |
| 周六 | 无新课 | 本周小结写入 `notes/`，欠账补齐 | — |
| 周日 | 休整 / 补进度 | — | — |

## 第 32 周：原始论文精读 —— Attention Is All You Need

**本周目标**：把你已经会写的架构和论文原文对上，论文图 1 里没有你不认识的组件。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 读论文《Attention Is All You Need》第 1-2 节（引言、背景） | 列出论文图 1 的全部模块 | 模块清单完成 |
| 周二 | 读第 3 节前段（encoder/decoder 堆叠、attention） | 对照你写的代码标注论文图 1 的左半部分 | 左半每个模块能对应到代码 |
| 周三 | 读第 3 节后段（FFN、位置编码等） | 标注图 1 右半部分 | 论文里没有你不认识的组件 |
| 周四 | 读论文第 4-5 节（训练设置、结果） | 开始整理"论文有但我没实现"的清单（位置编码等） | 清单列出至少 3 项 |
| 周五 | 选读：3b1b"GPT 如何存储知识" | 补全清单：label smoothing、warmup lr 等 | 清单完整 |
| 周六 | 无新课 | 给清单每项写一句动机，存 `notes/` | 每项能写一句动机 |
| 周日 | 休整 / 补进度 | — | — |

## 第 33 周：架构对比选读与周验收

**本周目标**：搞清自回归 vs 双向编码的适用场景，并通过位置编码周验收。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 选读：BERT vs GPT vs T5 架构对比前 1/2（任意一篇综述或 d2l 相关章节） | 记录三者架构差异要点 | 要点记入 `notes/` |
| 周二 | 选读：后 1/2 | 起草半页总结：自回归 vs 双向编码分别适合什么任务 | 初稿完成 |
| 周三 | 无新课 | 修改定稿 | 总结存 `notes/` |
| 周四 | 复习：回读论文位置编码一节 | 推导正弦位置编码公式 | 公式推导写进 `notes/` |
| 周五 | 复习：查 RoPE 概念（只要求名字和动机）/ 缓冲 | 整理"为什么 LLM 后来改用可学习/RoPE"的说法 | 能口头讲一遍 |
| 周六 | 无新课 | **周验收**：手写正弦位置编码公式并解释为什么 LLM 后来改用可学习/RoPE（后者只要求说出名字和动机） | 独立写出公式 |
| 周日 | 休整 / 补进度 | — | — |

## 第 34 周：GPT-2 复现精讲（上）—— 骨架与训练优化

**本周目标**：站在"能训练出 GPT-2 124M 的人"的视角，搭出 124M 骨架并理解训练优化分别解决什么瓶颈。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | Karpathy《Let's reproduce GPT-2 (124M)》（4h）第 1 小时前 30 分钟（代码走读） | 跟着在本地搭 GPT-2 124M 骨架的模型类 | 模型类写好、能实例化 |
| 周二 | 第 1 小时后 30 分钟 | 补全骨架（不训练），跑通前向 | 前向输出形状正确 |
| 周三 | 无新课 | 核对形状与参数量 | 参数量核对一致 = 124,439,808 |
| 周四 | 第 2 小时前 30 分钟（TF32、flash attn） | 记录当前 baseline 的 iter/s | baseline 数据记下 |
| 周五 | 第 2 小时后 30 分钟（torch.compile 等） | 把能用的优化逐项开起来，测前后 iter/s 对比 | 对比数据记下 |
| 周六 | 无新课 | 整理 MPS 上哪些优化可用、哪些不可用，存 `notes/` | 能说出每个优化改的是什么瓶颈 |
| 周日 | 休整 / 补进度 | — | — |

## 第 35 周：GPT-2 复现精讲（下）—— 数据、分布式与评估

**本周目标**：理解数据并行的本质，并亲手跑一次 HellaSwag 风格评估。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 第 3 小时前 30 分钟（FineWeb 数据） | 整理 FineWeb 数据处理要点到 `notes/` | 要点记录完成 |
| 周二 | 第 3 小时后 30 分钟（DDP 思想） | 读懂 DDP 的梯度 all-reduce 概念（单机不跑，画图理解） | 示意图画好 |
| 周三 | 无新课 | 用自己的话复述数据并行 | 能向人讲清数据并行为什么等价于大 batch |
| 周四 | 第 4 小时前 30 分钟（HellaSwag 评估） | 用 HF 加载 GPT-2 124M | 模型加载成功 |
| 周五 | 第 4 小时后 30 分钟（生成与采样） | 写 HellaSwag 风格的多选题评估脚本（小样例即可） | 脚本写好 |
| 周六 | 无新课 | 跑一次评估并看结果 | 理解"评估基准"在比什么 |
| 周日 | 休整 / 补进度 | — | — |

## 第 36 周：缓冲与阶段总验收

**本周目标**：从空文件独立写出完整可训练的字符级 GPT——这就是终点能力 ①。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 缓冲 / 补欠账；无欠账则直接开始验收 session 1 | 阶段总验收 session 1/6：从空文件起，写出 embedding + 单个 Block | 允许看论文不允许看视频 |
| 周二 | 无新课 | 验收 session 2/6：Block×N 堆叠 + 最终 LayerNorm | 同上 |
| 周三 | 无新课 | 验收 session 3/6：线性输出头 + 前向打通 | 同上 |
| 周四 | 无新课 | 验收 session 4/6：训练循环 | 同上 |
| 周五 | 无新课 | 验收 session 5/6：采样函数 | 同上 |
| 周六 | 无新课 | 验收 session 6/6：整体联调、跑通训练与采样 | **阶段总验收**：写出来能训练能采样——这就是终点能力 ① |
| 周日 | 休整 | 阶段三总结写入 `notes/` | — |

> 验收说明：原"限时 3 小时"拆为 6 个 30 分钟 session，周一至周六连续完成，总时长仍控制在 3 小时以内；全程允许看论文、不允许看视频。

## 第 36 周结束你应该具备

- [ ] 终点能力 ①：手写可训练的 GPT 并讲清每一层
- [ ] 读懂 nanoGPT 全部源码，理解工程化取舍
- [ ] 理解 BPE 分词并能手写最简实现
- [ ] 读过 Transformer 原始论文，能对照代码讲图 1
- [ ] 衔接阶段四：MiniMind 中文全流程 + LoRA 微调 + RAG 应用（见 `docs/LLM学习总路线.md` 第 37-48 周计划）
