# 前 4 周每日任务清单：PyTorch 核心（LLM 路线 · 阶段一）

> 本文档是 LLM 学习总路线（见 `docs/LLM学习总路线.md`）的阶段一：PyTorch 与张量计算基础。
> 每天 1~2 小时，照着做即可。每个任务都有明确的"看完什么 / 练什么 / 验收标准"。
> 环境激活方式（每次开终端先执行）：
> ```bash
> conda activate pytorch_env
> cd ~/code/llm_learn
> ```

## 本地资料位置

| 资料 | 位置 | 用法 |
|------|------|------|
| 动手学深度学习（d2l-zh，中文） | `~/code/llm_learn/d2l-zh/` | 直接读 `.md` 文件（含全部代码+讲解），也可在线看 zh.d2l.ai |
| PyTorch Handbook（thorough-pytorch） | `~/code/llm_learn/thorough-pytorch/notebook/` | 读 `第二章`、`第四章` 的 notebook |
| nanoGPT（阶段三用，第 9 周起） | `~/code/llm_learn/nanoGPT/` | 现在不用管 |
| 数据集 | `~/code/llm_learn/data/` | MNIST、CIFAR-10 已下载好，代码里写 `./data` 即可 |
| 配套练习代码 | `~/code/llm_learn/code/` | 已验证可运行，作为起点 |

在线资料（免费）：
- PyTorch 60 分钟入门：https://pytorch.org/tutorials/beginner/deep_learning_60min_blitz.html
- 李沐《动手学深度学习》B 站视频：搜索"李沐 动手学深度学习"，配套书就是本地的 d2l-zh
- 莫烦 PyTorch：B 站搜索"莫烦 PyTorch"

---

## 第 1 周：张量与自动微分

**本周目标**：像用 NumPy 一样操作张量，讲清楚 `requires_grad` 和 `backward()` 在干什么。

| 天 | 看（约 40-60 分钟） | 练（约 40-60 分钟） | 验收标准 |
|----|---------------------|---------------------|----------|
| 周一 | 60 分钟入门第 1 节 **Tensors** | 敲 `code/week1_practice.py` 的练习 1：shape/reshape/矩阵乘法；自己再造一个 `(2,3,4)` 张量，练习 squeeze/unsqueeze/sum(dim=) | 不查资料能说出 `view` 和 `reshape` 的区别 |
| 周二 | d2l 第 2.1-2.3 节（数据操作、数据预处理、线性代数）：`d2l-zh/chapter_preliminaries/ndarray.md` 等 | 把 2.1 的每个代码块都在 python 交互里敲一遍；重点练广播机制（`(3,1)+(1,4)` 会变成什么） | 能手算广播结果的形状 |
| 周三 | 60 分钟入门第 2 节 **Autograd** + d2l 2.5（自动求导） | 敲 `code/week1_practice.py` 练习 2；再做：对 `y=x·w+b`（x 随机）分别求 `y` 对 w、b 的梯度 | 练习 2 输出 8.0，能解释为什么 |
| 周四 | d2l 2.4（微积分：导数、偏导、链式法则），李沐 B 站对应视频 04 | 用 autograd 验证：`f(x)=sin(x)` 在 x=1 处的导数是否等于 `cos(1)`；再用 `detach`、`with torch.no_grad()` 各玩一次 | 能口头复述"链式法则和 backward() 的关系" |
| 周五 | 60 分钟入门第 3 节 **Neural Networks**（nn.Module 初见） | 把 `code/week2_mnist.py` 打开通读一遍（不用跑懂全部），划出看不懂的行，下周解决 | 能说出 nn.Module 里 `__init__` 和 `forward` 各管什么 |
| 周六 | 环境与 GPU：`torch.device`、`.to(device)` | 敲 `code/week1_practice.py` 练习 3；写个循环比较 1000×1000 矩阵乘法在 cpu 和 mps 上各跑 10 次的耗时 | 能报出 mps 比 cpu 快多少倍（通常 5-20 倍） |
| 周日 | 休整 / 补进度。仍有余力才看：莫烦 PyTorch 任选 5 集（每集约 10 分钟） | 把本周 3 个练习文件里的代码全部从头默写一遍（不抄） | 默写时卡壳的地方就是下周要重点补的 |

---

## 第 2 周：线性回归 → Softmax 回归

**本周目标**：不靠 nn.Module 的黑盒，亲手实现完整训练循环；理解损失函数、优化器、epoch 的含义。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | d2l 3.1-3.2（线性回归、从零实现）：`linear-regression.md`、`linear-regression-scratch.md` | 跟着书从零实现线性回归（自己造 y=2x+1+噪声 的数据），手写 loss、手写 SGD。**第一次手写训练循环，超时正常，做不完顺延到周日缓冲日** | 收敛到 w≈2, b≈1 |
| 周二 | d2l 3.3（简洁实现）+ 李沐视频 08、09 | 把周一的代码用 `nn.Linear`、`nn.MSELoss`、`torch.optim.SGD` 重写一遍 | 两版最终 loss 接近，能逐行对应 |
| 周三 | d2l 3.4（softmax 回归原理）+ 李沐视频 10 | 手推一遍 softmax 公式和交叉熵损失（纸上）；用 numpy 手写一个 softmax，测试极端输入不会溢出（减 max 技巧） | 能解释为什么交叉熵要和 softmax 搭配 |
| 周四 | d2l 3.5-3.7（图像分类数据集、softmax 从零实现、简洁实现） | 跟书跑通 FashionMNIST 分类。**注意：FashionMNIST 不在本地 `data/` 里，首次运行需联网自动下载** | 测试准确率 ≥ 80% |
| 周五 | 读通 `code/week2_mnist.py` 每一行（这就是周四代码的浓缩版） | 运行它（MPS 加速，每个 epoch 约 2-5 秒）；然后做实验：改 batch_size 为 8 和 512，对比 loss 曲线 | 能说出 batch_size 大小对训练速度和稳定性的影响 |
| 周六 | d2l 4.4（模型选择、欠拟合过拟合）+ 4.5（权重衰减） | 在周五代码上加实验：训练 50 个 epoch，观察训练/测试准确率何时分叉（过拟合现场） | 能指出过拟合发生的位置 |
| 周日 | 休整 / 补进度。**第 1-2 周总验收** | 关书从头写一遍"张量创建→线性回归训练循环"，限时 1 小时 | 写不出来就说明前面漏练了，回去补 |

---

## 第 3 周：MLP、DataLoader、模型保存

**本周目标**：独立写一个 `nn.Module` 子类，掌握 Dataset/DataLoader 全流程，跑通 MNIST 全连接网络。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | d2l 4.1-4.3（多层感知机、激活函数）+ 李沐视频 11 | 实现并画出 sigmoid / relu / tanh 曲线（matplotlib）；用 3 种激活函数各训练一次周四的模型 | 能说出为什么 ReLU 是默认选择 |
| 周二 | d2l 4.4-4.5 + thorough-pytorch `第二章` notebook 2.1-2.2 | 从零实现一个 784→256→10 的 MLP（继承 nn.Module）训练 MNIST | 测试准确率 ≥ 97% |
| 周三 | 60 分钟入门第 4 节 **Training a Classifier** + thorough-pytorch `第四章/4.1`（CIFAR-10 实战） | 读 CIFAR-10 代码，理解 `transforms` 在做什么；可视化 8 张训练图片 | 能说清 Normalize 的均值方差是干嘛的 |
| 周四 | d2l 4.6（Dropout）+ thorough-pytorch `第二章/2.4`（Dataset 和 DataLoader 深入） | 给 MLP 加 Dropout(0.2)，对比加与不加的过拟合程度；练习写一个自定义 Dataset 类（哪怕数据只是 10 个文件路径） | 自定义 Dataset 能被 DataLoader 正常迭代 |
| 周五 | d2l 4.8（数值稳定性与初始化）+ 模型保存加载（thorough-pytorch `第二章/2.5`） | 训练 MLP，`torch.save` 保存 → 新建进程加载 → 继续训练 5 个 epoch，准确率应无缝提升 | 断点续训跑通 |
| 周六 | 无新课，本周总验收 | **独立项目**：不看任何资料，从空文件写完整 MNIST 全流程（数据加载→MLP→训练→测试准确率→保存模型），可参考 `code/week2_mnist.py` 的结构但先关掉它 | 2 小时内独立完成，准确率 ≥ 96% |
| 周日 | 休整 / 补进度。有余力：d2l 6.1-6.2（为什么卷积、二维卷积层原理，为下周铺路） | 手算一个 3×3 卷积核对 5×5 输入做"valid"卷积的输出尺寸 | 能默写输出尺寸公式 ⌊(n−k+p+s)/s⌋ |

---

## 第 4 周：CNN 与 CIFAR-10 实战

**本周目标**：理解卷积、池化、批归一化，搭建一个 CNN 在 CIFAR-10 上达到 70%+ 准确率。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | d2l 6.1-6.3（卷积、填充步幅、多通道）+ 李沐视频 17、18 | 用 `nn.Conv2d` 对一张随机图做卷积，打印输出形状随 padding/stride 的变化 | 任何给定的输入尺寸+卷积参数，都能报出输出形状 |
| 周二 | d2l 6.4-6.6（池化、LeNet）+ 李沐视频 19 | 实现一个 2 conv + 2 pool + 2 fc 的 CNN 训练 MNIST | 准确率 ≥ 98%。对比第 3 周的 MLP：CNN 每个 epoch **更慢**（卷积计算量大），但用更少的 epoch 达到更高的准确率——理解"归纳偏置换样本效率" |
| 周三 | d2l 7.1-7.2（AlexNet、VGG 的设计思想）+ 李沐视频 20 | 在 CIFAR-10 上把周二的 CNN 改造一下（3 通道输入 32×32）跑起来 | 能跑完 1 个 epoch，loss 在下降 |
| 周四 | d2l 7.5（批归一化）+ 7.6（ResNet 思想即可，不必读完） | 给 CNN 加 BatchNorm2d；对比加与不加的收敛速度 | 加 BN 后 5 epoch 内达到的准确率明显高于不加 |
| 周五 | thorough-pytorch `第四章/4.2`（数据增强） | 加数据增强：RandomCrop(32, padding=4) + RandomHorizontalFlip，再训练 | 记录增强前后测试准确率对比（一般 +3~5%） |
| 周六 | 无新课，纯实验日 | 调参冲刺：学习率试 1e-2/1e-3/1e-4，epochs 20+，加 Dropout | CIFAR-10 测试准确率 ≥ 70%（16GB M4 完全够） |
| 周日 | 休整。**前 4 周总验收** | 写一页 markdown 总结：张量/autograd/训练循环/CNN 各用 3 句话讲给外行听，存到 `notes/` | 讲不清楚的地方回炉 |

---

## 通用规则

1. **代码必须手敲**，禁止 copy-paste 教程代码（运行本地模板文件除外）。
2. 每周留一个 **周日缓冲日**，进度落后时用来补，不落后时用来复盘。
3. 所有实验结果（准确率、loss 曲线截图）存到 `~/code/llm_learn/notes/`，攒 4 周的记录。
4. MPS 报错时：`export PYTORCH_ENABLE_MPS_FALLBACK=1`，或临时把 `.to("mps")` 改回 `.to("cpu")`。
5. 训练时内存吃紧：调小 `batch_size`，或 `torch.mps.empty_cache()`。
6. 遇到报错先自己读 traceback 15 分钟再搜——第 3 周的"独立项目"考验的就是这个能力。

## 第 4 周结束你应该具备

- [ ] 独立从零写"数据加载 → 模型定义 → 训练循环 → 评估 → 保存"全流程
- [ ] 看到任何报错能定位是数据形状、设备、还是梯度的问题
- [ ] 用 CNN 在 CIFAR-10 上拿到 70%+ 准确率
- [ ] 可以无缝衔接阶段二（第 5 周起）：Karpathy Zero to Hero，从零构建语言模型——见 `docs/LLM学习总路线.md` 与 `docs/阶段二每日计划.md`
