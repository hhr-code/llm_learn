# LLM 学习总路线（第一性原理优化版）

> 硬件：Mac M4 / 16GB / MPS。时间预算：每天约 30 分钟（每周约 3 小时，每 12 周一个阶段 ≈ 36 小时）。
> 本文档是总纲；每日任务见各阶段计划文档。所有阶段共用同一套机制：**每天"看→练→验收"，每周日缓冲/复盘，每阶段末关书重做**。

## 一、终点定义：什么叫"学会了 LLM"

学习路线必须先有可验证的终点，否则永远学不完。本路线的终点能力（约 12 个月后）：

1. **能手写**：从空文件写出一个可训练的 GPT（字符级，莎士比亚语料），并讲清每一层（embedding、attention、残差、LayerNorm、采样）在做什么、为什么。
2. **能复现**：在本地或云端跑通"预训练 → SFT 微调"全流程的小模型（MiniMind / nanoGPT 规模），知道 loss 曲线每一步意味着什么。
3. **能应用**：用 Hugging Face 生态完成一次真实微调（LoRA）和一个 RAG 小应用。

原路线（网上通用版）的问题是没有终点、没有验收，资料堆砌。本路线把一切都挂在上面三个终点上。

## 二、第一性原理取舍：为什么砍掉原路线的一半内容

原则：**只保留终点能力的因果链上的内容；数学按需学（just-in-time），不单独设阶段；重复资料只留一套。**

| 原路线内容 | 处理 | 理由 |
|---|---|---|
| 3Blue1Brown 数学全套（线代 16 集 + 微积分 12 集 + 概率） | **砍**，改为按需看 | 数学是工具不是门槛。阶段一训练中遇到链式法则/矩阵乘法时，看对应那几集即可，单独花数月学数学会严重推迟正反馈 |
| 吴恩达 ML Specialization、Fast.ai、Goodfellow《深度学习》 | **砍** | 与 d2l + Zero to Hero 内容高度重复，同一件事学三遍是最常见的半途而废原因 |
| Word2Vec、RNN/LSTM | **降级为选读** | 现代 LLM 关键路径上没有它们；makemore 第 5 讲（WaveNet）已覆盖序列建模的核心直觉 |
| BERT / T5 架构对比 | **压缩为 1 天阅读** | GPT 路线之外的知识，知道差异即可，不配练习 |
| nanochat 全流程复现 | **降级为云端可选** | 需 8×H100（约 $100），16GB Mac 物理上跑不了；作为阶段五的压轴可选项 |
| LangChain / LlamaIndex / 向量数据库全家桶 | **收敛** | 应用层只学 HF + 轻量方案（见阶段四），框架会过时，原理不会 |
| Karpathy Zero to Hero | **保留并升格为主线** | 唯一同时覆盖终点 1 和终点 2 的资料，难度递增设计极好 |
| MiniMind | **保留** | 中文 + 低资源全流程（预训练/SFT/DPO），正好适配 M4 16GB |
| 论文《Attention Is All You Need》 | **保留** | 但放在手写 GPT **之后**读——先会写再读论文，顺序反了会读不动 |

**阶段间依赖关系**（不能跳级的硬依赖）：

```
阶段一 PyTorch 核心 ──→ 阶段二 语言模型从零构建 ──→ 阶段三 Transformer/GPT ──→ 阶段四 全流程+应用
 (张量/autograd/训练循环)   (micrograd→makemore)        (手写GPT+分词器+论文)      (MiniMind/LoRA/RAG)
```

## 三、硬件现实（约束条件，不接受幻想）

| 任务 | M4 16GB 可行性 |
|---|---|
| nanoGPT 字符级莎士比亚（~1M 参数） | ✅ MPS 可跑，分钟级 |
| MiniMind 26M 预训练（小语料） | ✅ 可行，需调小 batch |
| GPT-2 124M 微调（LoRA） | ⚠️ 勉强，小 batch + 短序列 |
| GPT-2 从零预训练 | ❌ 需云端 GPU（AutoDL / Colab） |
| nanochat 完整流程 | ❌ 官方配置 8×H100，仅云端可选 |

**规则：本地跑不通的实验不硬跑，标记为"云端实验"，攒到阶段五统一处理。**

## 四、阶段总览

| 阶段 | 周 | 目标 | 阶段验收 | 计划文档 |
|---|---|---|---|---|
| 一：PyTorch 核心 | 1-12 | 张量/autograd/训练循环/CNN | 独立写 MNIST 全流程；CIFAR-10 ≥70% | `docs/前4周每日计划.md` |
| 二：语言模型从零构建 | 13-24 | micrograd → makemore 五讲 | 关视频重写 makemore MLP 并讲清反向传播 | `docs/阶段二每日计划.md` |
| 三：Transformer 与 GPT | 25-36 | 手写 GPT、分词器、读原始论文 | 从零写出可训练的字符级 GPT；跑通 nanoGPT | `docs/阶段三每日计划.md` |
| 四：全流程与应用 | 37-48 | MiniMind 中文全流程、LoRA、RAG | 完成一次真实微调 + 一个 RAG 小应用 | 见下方周计划 |
| 五：前沿深耕（长期可选） | 49+ | nanochat 云端复现、DPO/RLHF、部署 | — | 见下方说明 |

### 阶段四：周计划（第 37-48 周，30 分钟粒度）

阶段四对应原计划的第 13-16 周，按"原 1 周 → 3 周"展开，每天 30 分钟。四个验收里程碑分别落在第 39、42、45、48 周。

#### 第 37 周：MiniMind 项目与源码结构

| 天 | 看 | 练 | 验收标准 |
|---|---|---|---|
| 周一 | 读 `minimind/README.md` 前半部分（项目定位、模型规模、训练流程概览） | 在 `notes/` 里记下 MiniMind 的模型参数量与训练阶段划分 | 能说出 MiniMind 包含哪几个训练阶段 |
| 周二 | 读 `minimind/README.md` 后半部分（使用方法、目录说明） | 对照 `minimind/` 实际目录，画出目录结构草图 | 能指出 `model/`、`scripts/`、`dataset/` 各自职责 |
| 周三 | 读 `model/` 源码：embedding 与 attention 部分 | 对照你手写的 GPT，标注相同点 | 能指出 MiniMind attention 与你手写版本的对应关系 |
| 周四 | 读 `model/` 源码：残差、LayerNorm、输出头部分 | 继续对照，标注差异点候选 | 初步列出与手写 GPT 的差异候选清单 |
| 周五 | 重读源码中你没看懂的一处（任选） | 查资料或在 `notes/` 写下疑问与当前理解 | 疑问清单至少有一条自己的解释 |
| 周六 | 回顾本周笔记 | 整理源码对比笔记，写入 `notes/` | `notes/` 里有一份 MiniMind vs 手写 GPT 的结构对比 |
| 周日 | 缓冲/复盘 | — | — |

#### 第 38 周：环境安装与训练脚本

| 天 | 看 | 练 | 验收标准 |
|---|---|---|---|
| 周一 | 读 `minimind/requirements.txt`，了解每个依赖的用途 | 执行 `pip install -r minimind/requirements.txt` | 安装无报错；记下装了哪些新包 |
| 周二 | 读 `scripts/` 里 train_pretrain 脚本前半（参数与配置） | 在 `notes/` 列出关键超参数（batch_size、lr、epoch 等）及含义 | 能说出每个超参数的作用 |
| 周三 | 读 train_pretrain 脚本后半（数据加载与训练循环） | 对照阶段一写过的训练循环，找出三处相同结构 | 能讲清 MiniMind 训练循环与你写过版本的对应关系 |
| 周四 | 读数据准备部分（`dataset/` 说明与数据格式） | 下载/准备预训练小语料，确认格式正确 | 数据文件就位，能用 10 行内代码读出一条样本 |
| 周五 | 重读 train_pretrain 的 batch 与显存相关配置 | 调小 batch_size，写一份本地可跑的配置 | 配置文件保存好，能说出为什么调小 |
| 周六 | 回顾本周笔记 | 整理环境与配置笔记到 `notes/` | `notes/` 里有完整的"如何在本机跑 MiniMind 预训练"清单 |
| 周日 | 缓冲/复盘 | — | — |

#### 第 39 周：跑通 MiniMind 预训练小样例

| 天 | 看 | 练 | 验收标准 |
|---|---|---|---|
| 周一 | 快速回顾第 38 周的配置清单 | 启动预训练，观察前若干 step 的输出 | 训练能启动，无报错 |
| 周二 | 观察训练日志（loss、速度） | 继续跑训练，记录前 10 分钟的 loss 变化 | loss 数值被记录到 `notes/` |
| 周三 | 继续观察 loss 曲线走势 | 继续跑训练；若中断则从断点/重跑恢复 | loss 呈下降趋势 |
| 周四 | 读训练输出的 checkpoint 说明 | 确认 checkpoint 已保存，能用代码加载 | 能加载 checkpoint 并打印模型参数量 |
| 周五 | 回顾第 37 周的差异候选清单 | 结合跑通后的理解，定稿与手写 GPT 的三处差异 | 三处差异写入 `notes/`，每条有代码位置佐证 |
| 周六 | 整理本周记录 | **本周验收**：loss 正常下降，能说出与你手写 GPT 的三处差异 | 两条验收均达成并记录 |
| 周日 | 缓冲/复盘 | — | — |

#### 第 40 周：MiniMind SFT 准备

| 天 | 看 | 练 | 验收标准 |
|---|---|---|---|
| 周一 | 读 SFT 概念：预训练与 SFT 的区别（README 相关章节） | 在 `notes/` 用自己的话写出两者区别 | 能不看书讲清 SFT 解决什么问题 |
| 周二 | 读 SFT 训练脚本前半（参数与数据格式） | 列出 SFT 与预训练脚本的参数差异 | 差异清单写入 `notes/` |
| 周三 | 读 SFT 训练脚本后半（loss 计算部分） | 搞清 SFT 的 mask/标签处理，写两行注释说明 | 能讲清 SFT 的 loss 只在回答部分计算（或其实际机制） |
| 周四 | 读 SFT 数据格式说明 | 准备 SFT 小数据集，确认格式正确 | 能读出一条样本并解释字段含义 |
| 周五 | 重读 SFT 脚本中你不懂的一处 | 查资料或写疑问与解释到 `notes/` | 疑问至少有一条自己的解释 |
| 周六 | 回顾本周笔记 | 整理 SFT 启动前检查清单 | 清单含：数据、配置、预训练 checkpoint 路径 |
| 周日 | 缓冲/复盘 | — | — |

#### 第 41 周：跑通 SFT 与对话评测

| 天 | 看 | 练 | 验收标准 |
|---|---|---|---|
| 周一 | 回顾上周检查清单 | 加载第 39 周的预训练 checkpoint，启动 SFT | SFT 训练能启动，无报错 |
| 周二 | 观察 SFT 训练日志 | 继续跑 SFT，记录 loss 变化 | loss 记录到 `notes/` |
| 周三 | 继续观察 loss 走势 | 继续跑 SFT 至完成或达到可接受的 loss | SFT checkpoint 保存成功 |
| 周四 | 读 `eval_llm.py` 的用法说明 | 用 `eval_llm.py` 加载 SFT 模型进行对话 | 模型能生成连贯回复 |
| 周五 | 读 `eval_llm.py` 源码，理解生成参数（temperature 等） | 换 2-3 个 prompt 测试，记录输出差异 | 能说出 temperature 对输出的影响 |
| 周六 | 回顾本周记录 | 把 SFT 过程与关键截图/日志整理进 `notes/` | `notes/` 有完整 SFT 实验记录 |
| 周日 | 缓冲/复盘 | — | — |

#### 第 42 周：微调前后对比与 DPO 选读

| 天 | 看 | 练 | 验收标准 |
|---|---|---|---|
| 周一 | 回顾微调概念 | 用同一 prompt 分别测试预训练模型与 SFT 模型，记录输出 | 两组输出都保存下来 |
| 周二 | 分析两组输出的差异 | 把对比写进 `notes/`（prompt、两个输出、你的点评） | 对比笔记完成 |
| 周三 | 再选 2 个不同类型的 prompt | 重复对比实验，补充进 `notes/` | 至少 3 组 prompt 的对比 |
| 周四 | 选读：`minimind` 的 DPO 脚本前半 | 记录 DPO 与 SFT 的数据格式差异 | 能说出 DPO 数据比 SFT 多了什么 |
| 周五 | 选读：DPO 脚本后半 | 用自己的话写 DPO 的目标（不懂就写疑问） | `notes/` 有一段 DPO 笔记或疑问清单 |
| 周六 | 回顾本周 | **本周验收**：微调前后同一 prompt 的输出对比，写进 `notes/` | 对比报告完整，含你自己的分析 |
| 周日 | 缓冲/复盘 | — | — |

#### 第 43 周：HF 生态——加载开源小模型

| 天 | 看 | 练 | 验收标准 |
|---|---|---|---|
| 周一 | 了解 Hugging Face 生态（transformers 库的核心概念） | 装好/确认 `transformers` 可用，跑通官方 quickstart 示例 | 能加载任意一个小模型并生成文本 |
| 周二 | 读 Qwen 0.5B 的模型卡（用途、协议、硬件要求） | 下载/加载 Qwen 0.5B（首选 ModelScope，不可用再用 HF） | 模型加载成功，能打印参数量 |
| 周三 | 读 tokenizer 的用法（encode/decode、chat template） | 对同一句话做 encode → decode，观察 token 序列 | 能解释 chat template 的作用 |
| 周四 | 读 generate 的关键参数（max_new_tokens、do_sample 等） | 用 Qwen 0.5B 做几轮对话测试 | 输出连贯，参数作用能讲清 |
| 周五 | 对比 MiniMind 与 Qwen 的使用方式差异 | 把对比写进 `notes/` | 能说出 HF pipeline 与手写 generate 的对应关系 |
| 周六 | 回顾本周 | 整理"HF 加载与推理"速查笔记到 `notes/` | 速查笔记可复用（下次照做能跑通） |
| 周日 | 缓冲/复盘 | — | — |

#### 第 44 周：PEFT / LoRA 原理与训练启动

| 天 | 看 | 练 | 验收标准 |
|---|---|---|---|
| 周一 | 学 LoRA 原理（低秩矩阵注入，看一篇通俗介绍或 HF PEFT 文档） | 用自己的话在 `notes/` 写出 LoRA 为什么省显存 | 能画出 LoRA 的结构示意 |
| 周二 | 读 PEFT 库的 LoraConfig 参数（r、alpha、target_modules） | 在 `notes/` 列出每个参数的含义 | 能说出 r 大小对训练的影响 |
| 周三 | 读一个 LoRA 微调示例脚本（HF 官方或社区小模型示例） | 对照示例，确定你的微调数据格式 | 数据格式确定，样本准备好 |
| 周四 | 重读示例的训练循环部分 | 写/改出自己的 LoRA 训练脚本（手敲） | 脚本能通过语法检查 |
| 周五 | 检查脚本的显存相关配置 | 调小 batch、缩短序列，启动 LoRA 训练 | 训练能启动，无 OOM |
| 周六 | 观察训练日志 | 继续跑训练，记录 loss | loss 呈下降趋势并记录到 `notes/` |
| 周日 | 缓冲/复盘 | — | — |

#### 第 45 周：完成 LoRA 训练与保存 adapter

| 天 | 看 | 练 | 验收标准 |
|---|---|---|---|
| 周一 | 观察训练进展 | 继续跑 LoRA 训练 | 训练持续推进，无报错 |
| 周二 | 观察 loss 是否收敛 | 继续跑或结束训练，确认最终 loss | 最终 loss 记录到 `notes/` |
| 周三 | 读 adapter 保存/加载的 API 文档 | 保存 adapter，确认文件落盘 | adapter 文件存在，大小远小于全模型 |
| 周四 | 读"基座模型 + adapter 合并加载"的用法 | 重新加载基座 + adapter 做推理 | 加载成功，输出正常 |
| 周五 | 设计 2-3 个测试 prompt | 对比微调前后输出，记录差异 | 能看到微调带来的行为变化 |
| 周六 | 回顾本周 | **本周验收**：跑通一次 LoRA 训练并保存 adapter | adapter 已保存，加载推理验证通过 |
| 周日 | 缓冲/复盘 | — | — |

#### 第 46 周：RAG——文档向量化

| 天 | 看 | 练 | 验收标准 |
|---|---|---|---|
| 周一 | 学 RAG 基本思想（检索增强生成，看一篇通俗介绍） | 用自己的话在 `notes/` 画出 RAG 流程图 | 能讲清"先检索后生成"的意义 |
| 周二 | 读 sentence-transformers 的快速上手文档 | 装好后对 3 句话算 embedding，打印维度 | 能得到向量并解释维度含义 |
| 周三 | 读 embedding 相似度的计算方法 | 计算 3 句话两两余弦相似度，验证直觉 | 相似句分数高、不相似句分数低 |
| 周四 | 设计 `notes/` 文档的切分策略（按段/按标题） | 写脚本把自己的 `notes/` 文档切成小块 | 切分结果合理，每块有来源标注 |
| 周五 | 批量向量化的注意事项（批处理、速度） | 对所有文档块计算 embedding 并保存 | 向量文件保存成功 |
| 周六 | 回顾本周 | 整理向量化脚本与笔记 | 脚本可重跑，`notes/` 有流程记录 |
| 周日 | 缓冲/复盘 | — | — |

#### 第 47 周：RAG——FAISS 检索与生成

| 天 | 看 | 练 | 验收标准 |
|---|---|---|---|
| 周一 | 读 FAISS 的基本概念（索引、最近邻搜索） | 安装 FAISS，用第 46 周的向量建索引 | 索引建成，能存盘/读盘 |
| 周二 | 读 FAISS 的 search API | 对一个测试问题检索 top-k 文档块 | 返回结果含相似度分数 |
| 周三 | 调 k 值与结果质量的关系 | 试 k=1/3/5，比较检索结果 | 能说出 k 的取舍 |
| 周四 | 设计"检索结果 → prompt"的拼接模板 | 写把检索块塞进 prompt 的函数（手敲） | 拼接结果格式正确 |
| 周五 | 选一个生成模型（Qwen 0.5B 或微调后的模型） | 把拼接后的 prompt 喂给模型生成回答 | 能产出完整回答 |
| 周六 | 回顾本周 | 整理 RAG 管线代码与笔记 | 端到端管线能一键跑通 |
| 周日 | 缓冲/复盘 | — | — |

#### 第 48 周：RAG 应用验收与阶段总结

| 天 | 看 | 练 | 验收标准 |
|---|---|---|---|
| 周一 | 回顾 RAG 管线 | 对自己的 `notes/` 目录做 3 个问题的问答，记录结果 | 回答与 `notes/` 内容相关 |
| 周二 | 分析回答质量（哪里引对了、哪里引错了） | 记录 bad case 并尝试调整（切分/k 值/prompt） | 至少改进一个 bad case |
| 周三 | 再测 2-3 个不同类型的问题 | 记录结果，评估整体可用性 | 问答结果整体合理 |
| 周四 | 回顾阶段四全部笔记 | 把 RAG 小应用代码整理成可复用脚本 | 脚本有简短使用说明 |
| 周五 | 回顾四个里程碑（39/42/45/48 周）的验收记录 | 写阶段四总结：学到了什么、哪里还薄弱 | 总结写入 `notes/` |
| 周六 | — | **本周验收**：对自己的 `notes/` 目录做一次问答，结果合理 | 问答演示通过，结果与分析存档 |
| 周日 | 缓冲/复盘 | — | — |

### 阶段五：长期可选（第 49 周起，不设时间表）

- **nanochat 云端复现**（压轴）：`nanochat/` 已在本地，先读 `README.md`；需要时租 8×H100（约 $100）跑完整流程：Rust 分词器 → FineWeb 预训练 → SFT → 可选 GRPO → WebUI。
- **方向深耕**：对齐（DPO/RLHF，可读 minimind 的 DPO 脚本入门）、推理部署（Ollama 本地跑开源模型、vLLM）、多模态。
- **信息源**：arXiv、Hugging Face 社区、Karpathy 的新项目。

## 五、环境与资料清单

| 环境/资料 | 位置 | 用于 |
|---|---|---|
| `pytorch_env`（conda） | 已装好 | 阶段一 |
| `llm_env`（conda） | 已装好（torch + transformers + datasets + tiktoken + jupyter） | 阶段二起，激活：`conda activate llm_env` |
| d2l-zh | `d2l-zh/` | 阶段一主教材，后续按需查 |
| thorough-pytorch | `thorough-pytorch/` | 阶段一辅助 |
| Zero to Hero 代码 | `nn-zero-to-hero/lectures/` | 阶段二、三主线（视频在 YouTube/B 站搜 "Karpathy Zero to Hero"） |
| minbpe | `minbpe/` | 阶段三分词器练习 |
| nanoGPT | `nanoGPT/` | 阶段三实战 |
| MiniMind | `minimind/` | 阶段四主线 |
| nanochat | `nanochat/` | 阶段五可选（云端） |
| 数据集 | `data/` | MNIST / CIFAR-10 |
| 实验记录 | `notes/` | 每阶段的 loss 曲线、对比实验、总结都存这里 |

## 六、通用规则（继承自阶段一，全程适用）

1. 代码必须手敲；视频可以 1.25-1.5 倍速，但代码必须自己跟着写。
2. 每天看与练约各占一半；看视频超过当天一半时间就说明今天没练够。
3. 遇到报错先读 traceback 15 分钟再搜。
4. 每周日缓冲；每阶段最后一天"关书重做"——做不出就回炉，不带病进入下一阶段。
