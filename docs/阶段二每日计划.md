# 阶段二每日任务清单：语言模型从零构建（第 5-8 周）

> 主线：Karpathy《Neural Networks: Zero to Hero》第 1-6 讲（视频在 YouTube/B 站，配套代码在本地 `nn-zero-to-hero/lectures/`）。
> 环境：`conda activate llm_env && cd ~/code/llm_learn`
> 学习方式：**边看边在自己的 notebook/脚本里重写代码**，不要直接运行仓库里的现成 notebook。
> 直觉补充（可选，每天不超过 20 分钟）：3Blue1Brown《神经网络的本质》第 1-4 集。

## 第 5 周：micrograd —— 手写自动微分引擎

**本周目标**：亲手实现反向传播，彻底理解 `backward()` 里面发生了什么（第 1 周学的 autograd 在这里"开盖"）。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 第 1 讲 micrograd 视频前 1/3（导数、链式法则直觉，视频全长约 2.5h） | 在 `code/week5_micrograd.py` 里跟着写 `Value` 类和 `+`、`*` 的局部梯度 | 能对 `a*b+c` 手算每个节点的梯度并与代码输出一致 |
| 周二 | 视频中段（tanh 神经元、完整 backward） | 写完 `tanh`、`backward()` 的拓扑排序实现 | 单个神经元的梯度与手算一致 |
| 周三 | 视频后段（与 PyTorch 对照） | 用 `torch.autograd` 验证你的 micrograd 梯度完全相等 | 两套梯度数值差 < 1e-6 |
| 周四 | 重看没跟上的部分；读 `nn-zero-to-hero/lectures/micrograd/` 源码对照 | 给 micrograd 补上 `-`、`/`、`**` 运算（视频练习） | 补全后跑通视频里的全部示例 |
| 周五 | 3b1b《神经网络的本质》第 3-4 集（梯度下降、反向传播，可选） | 用 micrograd 手写一个 2 层 MLP 训练二分类（moons 数据，视频结尾练习） | 决策边界可视化，loss 收敛 |
| 周六 | 无新课 | **周验收**：关掉所有资料，从空文件重写 `Value` 类 + backward，限时 90 分钟 | 独立写出，梯度验证通过 |
| 周日 | 休整 / 补进度 | 本周卡点记入 `notes/` | — |

## 第 6 周：makemore 1-2 —— 从 bigram 到 MLP 语言模型

**本周目标**：建立"语言模型 = 预测下一个 token 的分类问题"的第一性认知；理解 embedding。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 第 2 讲 makemore bigram 视频前 1/2（计数值统计、采样） | 跟着用纯 Python + torch 张量统计 bigram 频率并采样"人名" | 能解释为什么采样结果是乱码但像名字 |
| 周二 | 视频后 1/2（用神经网络 + 负对数似然重做 bigram） | 手写单层的 bigram 网络，梯度下降训练 | 神经网络的 loss 与计数法的理论最优 loss 接近 |
| 周三 | 第 3 讲 MLP（Bengio 2003 思想）前 1/2 | 实现 embedding 表 + 上下文拼接（context=3）的前向 | 能画出"字符 → 向量 → 拼接 → 隐藏层 → softmax"的数据流 |
| 周四 | 视频后 1/2（训练 + train/val/test 划分） | 完整训练 makemore MLP，调 embedding 维度、隐藏层大小 | val loss ≤ 视频中的对应水平；能说出为什么必须划分三个集合 |
| 周五 | 选读：d2l 8.1-8.2（语言模型与数据集、文本预处理） | 把名字数据集换成任意中文词表（拼音或单字），跑通同一套代码 | 数据管线不依赖原数据集，能换料 |
| 周六 | 无新课 | **周验收**：给别人（或对自己录音）讲清"NLL 损失和交叉熵是同一件事"，并推一遍 | 讲不顺就重看第 2 讲 |
| 周日 | 休整 / 补进度 | 实验记录存 `notes/` | — |

## 第 7 周：makemore 3-4 —— 初始化、BatchNorm、手动反向传播

**本周目标**：理解深层网络训不动的根因（初始化/激活分布），并拥有"徒手推梯度"的能力——这是后面读 Transformer 论文的硬功夫。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 第 3 讲前 1/2（激活值分布、死亡 tanh、Kaiming 初始化） | 跟着画激活值/梯度的直方图，观察饱和 | 能解释 `w * (5/3) / sqrt(fan_in)` 每个因子的作用 |
| 周二 | 视频后 1/2（BatchNorm 原理与实现） | 给 makemore MLP 加手写 BatchNorm（不用 nn.BatchNorm1d） | 加 BN 后训练对初始化不再敏感 |
| 周三 | 第 4 讲 Backprop Ninja 前 1/2（逐层手推梯度） | 纸上推 linear、tanh、softmax 层的梯度，再写代码 | 每层手写梯度与 autograd 对拍通过 |
| 周四 | 视频后 1/2（cross entropy、BN 的梯度） | 推完全部层的反向传播 | 全部 `cmp` 检查通过（视频里的对照函数） |
| 周五 | 选读：d2l 4.8（数值稳定性与初始化，第 3 周读过，现在重读会有新理解） | 把 makemore 的 BN 换成 LayerNorm，对比差异 | 能说出 BN 与 LN 归一化维度的区别（Transformer 用 LN，这是伏笔） |
| 周六 | 无新课 | **周验收**：默写 cross-entropy loss 对 logits 的梯度推导（结果极简洁），限时 30 分钟 | 独立推出 ∂L/∂logits = p − y |
| 周日 | 休整 / 补进度 | 实验记录存 `notes/` | — |

## 第 8 周：makemore 5 —— 层次化与卷积化 + 阶段总验收

**本周目标**：看到"把网络变深/变宽的正确姿势"，理解 Torch 模块化的设计（为读 nanoGPT 源码做铺垫）。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 第 5 讲 WaveNet 前 1/2（把 MLP 改写成 nn.Module 风格） | 跟着把网络重构成 `Module/Layer/Sequential` 框架 | 理解 PyTorch API 设计动机，能自己实现简化版 `nn.Module` |
| 周二 | 视频后 1/2（膨胀卷积/树状结构） | 实现 context=8 的层次化 makemore | val loss 比第 6 周的 MLP 更低 |
| 周三 | 选读：d2l 8.3-8.5（语言模型进阶，RNN 一节只读概念不实现） | 整理一张"bigram → MLP → WaveNet → ?"的演进图，最后一格留给 Transformer | 能说出每一步解决了什么问题、还剩什么问题（长程依赖） |
| 周四 | 缓冲 / 补前三周欠账 | — | — |
| 周五 | 缓冲 / 补前三周欠账 | — | — |
| 周六 | 无新课 | **阶段总验收**：关掉视频和仓库，从空文件重写 makemore MLP 版（embedding + 隐藏层 + 训练 + 采样），限时 2 小时 | 独立写完并能训练；然后口头讲清反向传播的每一步 |
| 周日 | 休整 | 阶段二总结写入 `notes/`：micrograd/makemore 各 3 句话 | 讲不清的回炉对应那一周 |

## 第 8 周结束你应该具备

- [ ] 能手写自动微分引擎并解释拓扑排序反向传播
- [ ] 能手推常见层的梯度（这是阶段三读论文和手写 attention 的前置）
- [ ] 理解语言模型 = 下一个 token 预测，embedding/NLL/采样全链路
- [ ] 可以无缝衔接阶段三：手写 Transformer/GPT（`docs/阶段三每日计划.md`）
