# 阶段二每日任务清单：语言模型从零构建（第 13-24 周）

> 时间预算：**每天约 30 分钟（每周约 3 小时）**，每周 6 个学习日 + 周日缓冲/复盘的机制不变。
> 主线：Karpathy《Neural Networks: Zero to Hero》第 1-6 讲（视频在 YouTube/B 站，配套代码在本地 `nn-zero-to-hero/lectures/`）。
> 环境：`conda activate llm_env && cd ~/code/llm_learn`
> 学习方式：**边看边在自己的 notebook/脚本里重写代码**，不要直接运行仓库里的现成 notebook。
> 直觉补充（可选，不占主线 30 分钟，每天不超过 20 分钟）：3Blue1Brown《神经网络的本质》第 1-4 集。

## 第 13 周：micrograd ① —— 导数直觉与 `Value` 类

**本周目标**：开始手写自动微分引擎，理解反向传播的链式法则直觉（第 1-12 周学的 autograd 在这里"开盖"）。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 第 1 讲 micrograd 视频前 1/6（导数的直觉，视频全长约 2.5h） | 无（纯看 + 随手笔记） | 能用自己的话说出"导数是局部变化率" |
| 周二 | 看视频第 2/6（链式法则直觉） | 纸上推一遍简单表达式的链式法则 | 能解释链式法则如何把局部梯度连成全局梯度 |
| 周三 | 无新课（回看周二内容 10 分钟） | 在 `code/week5_micrograd.py` 里跟着写 `Value` 类和 `+`、`*` 的局部梯度 | 能对 `a*b+c` 手算每个节点的梯度并与代码输出一致 |
| 周四 | 看视频第 3/6（tanh 神经元） | 无（纯看 + 随手笔记） | 能说出 tanh 的导数形式 |
| 周五 | 看视频第 4/6（完整 backward 讲解） | 无（纯看 + 随手笔记） | 能解释拓扑排序为什么必要 |
| 周六 | 无新课 | 写完 `tanh`、`backward()` 的拓扑排序实现 | 单个神经元的梯度与手算一致 |
| 周日 | 休整 / 补进度 | 本周卡点记入 `notes/` | — |

## 第 14 周：micrograd ② —— 与 PyTorch 对照、补全运算

**本周目标**：验证你的 micrograd 与 PyTorch autograd 数值完全一致，并补全缺失的运算。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 看视频第 5/6（与 PyTorch 对照部分） | 无（纯看 + 随手笔记） | 能说出两套引擎接口的对应关系 |
| 周二 | 看视频剩余部分，补齐没看的片段 | 无（纯看 + 随手笔记） | 整讲视频过完一遍 |
| 周三 | 无新课 | 用 `torch.autograd` 验证你的 micrograd 梯度完全相等 | 两套梯度数值差 < 1e-6 |
| 周四 | 重看没跟上的部分；读 `nn-zero-to-hero/lectures/micrograd/` 源码前半 | 无（纯读源码对照） | 能指出自己实现与源码的差异 |
| 周五 | 读 `nn-zero-to-hero/lectures/micrograd/` 源码后半对照 | 无（纯读源码对照） | 能解释源码里 `_backward` 闭包的写法 |
| 周六 | 无新课 | 给 micrograd 补上 `-`、`/`、`**` 运算（视频练习） | 补全后跑通视频里的全部示例 |
| 周日 | 休整 / 补进度 | 本周卡点记入 `notes/` | — |

## 第 15 周：micrograd ③ —— MLP 实战 + 周验收

**本周目标**：用自己的 micrograd 训练一个小 MLP，并通过独立重写验收。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 3b1b《神经网络的本质》第 3 集（梯度下降，可选） | 无（纯看 + 随手笔记） | 能口述梯度下降更新公式 |
| 周二 | 3b1b《神经网络的本质》第 4 集（反向传播，可选） | 无（纯看 + 随手笔记） | 能把 3b1b 的图示对应到 micrograd 的计算图 |
| 周三 | 无新课 | 用 micrograd 手写一个 2 层 MLP 训练二分类（moons 数据，视频结尾练习） | 决策边界可视化，loss 收敛 |
| 周四 | 无新课 | **周验收 ①**：关掉所有资料，从空文件开始重写 `Value` 类骨架（30 分钟，第 1 段） | 写出 `Value` 数据结构，存盘留待明天继续 |
| 周五 | 无新课 | **周验收 ②**：接着写 `+`、`*`、`tanh` 运算及局部梯度（30 分钟，第 2 段） | 运算齐全，前向结果正确 |
| 周六 | 无新课 | **周验收 ③**：写完 `backward()` 并做梯度验证（30 分钟，第 3 段） | 独立写出，梯度验证通过（原限时 90 分钟拆为周四~周六 3 个 session 连续完成） |
| 周日 | 休整 / 补进度 | 本周卡点记入 `notes/` | — |

## 第 16 周：makemore ① —— bigram 语言模型

**本周目标**：建立"语言模型 = 预测下一个 token 的分类问题"的第一性认知。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 第 2 讲 makemore bigram 视频前 1/4（数据集与计数思路） | 无（纯看 + 随手笔记） | 能说出 bigram 模型的假设 |
| 周二 | 看视频第 2/4（计数值统计、采样） | 无（纯看 + 随手笔记） | 能解释计数表如何变成概率分布 |
| 周三 | 无新课 | 跟着用纯 Python + torch 张量统计 bigram 频率并采样"人名" | 能解释为什么采样结果是乱码但像名字 |
| 周四 | 看视频第 3/4（负对数似然 NLL） | 无（纯看 + 随手笔记） | 能写出 NLL 的公式 |
| 周五 | 看视频第 4/4（用神经网络重做 bigram） | 无（纯看 + 随手笔记） | 能说出 one-hot × W 等价于查表 |
| 周六 | 无新课 | 手写单层的 bigram 网络，梯度下降训练 | 神经网络的 loss 与计数法的理论最优 loss 接近 |
| 周日 | 休整 / 补进度 | 实验记录存 `notes/` | — |

## 第 17 周：makemore ② —— MLP 语言模型与 embedding

**本周目标**：理解 embedding，完成 makemore MLP 的完整训练与调参。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 第 3 讲 MLP（Bengio 2003 思想）前 1/4 | 无（纯看 + 随手笔记） | 能说出 Bengio 2003 要解决 bigram 的什么问题 |
| 周二 | 看视频第 2/4（embedding 与网络结构） | 无（纯看 + 随手笔记） | 能解释 embedding 表的作用 |
| 周三 | 无新课 | 实现 embedding 表 + 上下文拼接（context=3）的前向 | 能画出"字符 → 向量 → 拼接 → 隐藏层 → softmax"的数据流 |
| 周四 | 看视频第 3/4（训练流程） | 无（纯看 + 随手笔记） | 能复述训练循环的每一步 |
| 周五 | 看视频第 4/4（train/val/test 划分） | 无（纯看 + 随手笔记） | 能说出三个集合各自的用途 |
| 周六 | 无新课 | 完整训练 makemore MLP，调 embedding 维度、隐藏层大小 | val loss ≤ 视频中的对应水平；能说出为什么必须划分三个集合 |
| 周日 | 休整 / 补进度 | 实验记录存 `notes/` | — |

## 第 18 周：makemore ③ —— 数据管线迁移 + 周验收

**本周目标**：把数据管线从英文人名迁移到中文，并通过"讲出来"验收。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 选读：d2l 8.1（语言模型与数据集） | 无（纯读 + 随手笔记） | 能说出困惑度与交叉熵的关系 |
| 周二 | 选读：d2l 8.2（文本预处理） | 无（纯读 + 随手笔记） | 能列出文本预处理的标准步骤 |
| 周三 | 无新课 | 把名字数据集换成任意中文词表（拼音或单字），跑通同一套代码 | 数据管线不依赖原数据集，能换料 |
| 周四 | 无新课 | **周验收 ①**：列提纲，录音试讲"NLL 损失和交叉熵是同一件事"的第一遍 | 录下完整一遍，找出讲不顺的地方 |
| 周五 | 无新课 | **周验收 ②**：对讲不顺的部分重看第 2 讲对应段落 | 卡壳点逐一解决 |
| 周六 | 无新课 | **周验收 ③**：给别人（或对自己录音）完整讲一遍，并把推导推完 | 讲不顺就重看第 2 讲 |
| 周日 | 休整 / 补进度 | 实验记录存 `notes/` | — |

## 第 19 周：makemore ④ —— 初始化与 BatchNorm

**本周目标**：理解深层网络训不动的根因（初始化/激活分布）。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 第 3 讲（初始化部分）前 1/4（激活值分布） | 无（纯看 + 随手笔记） | 能说出激活值分布失控的两种形态 |
| 周二 | 看视频第 2/4（死亡 tanh、Kaiming 初始化） | 无（纯看 + 随手笔记） | 能解释"死亡神经元"的成因 |
| 周三 | 无新课 | 跟着画激活值/梯度的直方图，观察饱和 | 能解释 `w * (5/3) / sqrt(fan_in)` 每个因子的作用 |
| 周四 | 看视频第 3/4（BatchNorm 原理） | 无（纯看 + 随手笔记） | 能说出 BN 在训练和推理时的差异 |
| 周五 | 看视频第 4/4（BatchNorm 实现） | 无（纯看 + 随手笔记） | 能复述 BN 的前向计算步骤 |
| 周六 | 无新课 | 给 makemore MLP 加手写 BatchNorm（不用 nn.BatchNorm1d） | 加 BN 后训练对初始化不再敏感 |
| 周日 | 休整 / 补进度 | 实验记录存 `notes/` | — |

## 第 20 周：makemore ⑤ —— 手动反向传播（Backprop Ninja）

**本周目标**：拥有"徒手推梯度"的能力——这是后面读 Transformer 论文的硬功夫。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 第 4 讲 Backprop Ninja 前 1/4 | 无（纯看 + 随手笔记） | 能说出本讲与第 1 讲的衔接关系 |
| 周二 | 看视频第 2/4（逐层手推梯度） | 无（纯看 + 随手笔记） | 能复述 broadcasting 在梯度里的陷阱 |
| 周三 | 无新课 | 纸上推 linear、tanh、softmax 层的梯度，再写代码 | 每层手写梯度与 autograd 对拍通过 |
| 周四 | 看视频第 3/4（cross entropy 的梯度） | 无（纯看 + 随手笔记） | 能说出 CE 梯度为何极简 |
| 周五 | 看视频第 4/4（BN 的梯度） | 无（纯看 + 随手笔记） | 能复述 BN 梯度的推导主线 |
| 周六 | 无新课 | 推完全部层的反向传播 | 全部 `cmp` 检查通过（视频里的对照函数） |
| 周日 | 休整 / 补进度 | 实验记录存 `notes/` | — |

## 第 21 周：makemore ⑥ —— LayerNorm 对比 + 周验收

**本周目标**：对比 BN 与 LN，并通过默写梯度推导验收。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 重读 d2l 4.8（数值稳定性与初始化，前面读过，现在重读会有新理解） | 无（纯读 + 随手笔记） | 能说出重读后新理解到的一点 |
| 周二 | 无新课 | 把 makemore 的 BN 换成 LayerNorm，跑通训练 | LN 版训练正常收敛 |
| 周三 | 无新课 | 对比 BN 与 LN 的训练曲线与效果差异，记入 `notes/` | 能说出 BN 与 LN 归一化维度的区别（Transformer 用 LN，这是伏笔） |
| 周四 | 无新课 | 对照笔记，完整过一遍 cross-entropy → softmax → logits 的链式推导 | 推导链上每一步都能说清 |
| 周五 | 无新课 | 对卡壳环节重看第 4 讲对应片段 | 卡壳点逐一解决 |
| 周六 | 无新课 | **周验收**：默写 cross-entropy loss 对 logits 的梯度推导（结果极简洁），限时 30 分钟 | 独立推出 ∂L/∂logits = p − y |
| 周日 | 休整 / 补进度 | 实验记录存 `notes/` | — |

## 第 22 周：makemore ⑦ —— WaveNet：层次化与卷积化

**本周目标**：看到"把网络变深/变宽的正确姿势"，理解 Torch 模块化的设计（为读 nanoGPT 源码做铺垫）。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 第 5 讲 WaveNet 前 1/4 | 无（纯看 + 随手笔记） | 能说出本讲相对前几讲的升级点 |
| 周二 | 看视频第 2/4（把 MLP 改写成 nn.Module 风格） | 无（纯看 + 随手笔记） | 能说出 Module/Layer/Sequential 各自的职责 |
| 周三 | 无新课 | 跟着把网络重构成 `Module/Layer/Sequential` 框架 | 理解 PyTorch API 设计动机，能自己实现简化版 `nn.Module` |
| 周四 | 看视频第 3/4（膨胀卷积） | 无（纯看 + 随手笔记） | 能画出膨胀卷积的感受野 |
| 周五 | 看视频第 4/4（树状结构） | 无（纯看 + 随手笔记） | 能解释树状结构为何省计算 |
| 周六 | 无新课 | 实现 context=8 的层次化 makemore | val loss 比第 16-17 周的 MLP 更低 |
| 周日 | 休整 / 补进度 | 实验记录存 `notes/` | — |

## 第 23 周：makemore ⑧ —— 演进图整理 + 缓冲

**本周目标**：把 bigram → MLP → WaveNet 的演进脉络整理成图，明确留给 Transformer 的问题。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 选读：d2l 8.3-8.4（语言模型进阶） | 无（纯读 + 随手笔记） | 能说出长程依赖问题是什么 |
| 周二 | 选读：d2l 8.5（RNN 一节只读概念不实现） | 无（纯读 + 随手笔记） | 能说出 RNN 处理长程依赖的瓶颈 |
| 周三 | 无新课 | 整理一张"bigram → MLP → WaveNet → ?"的演进图，最后一格留给 Transformer | 能说出每一步解决了什么问题、还剩什么问题（长程依赖） |
| 周四 | 缓冲 / 补前几周欠账 | — | — |
| 周五 | 缓冲 / 补前几周欠账 | — | — |
| 周六 | 缓冲 / 补前几周欠账 | — | — |
| 周日 | 休整 / 补进度 | 实验记录存 `notes/` | — |

## 第 24 周：阶段二总验收

**本周目标**：不看任何资料，独立重建 makemore MLP，完成阶段二的封口验收。

| 天 | 看 | 练 | 验收标准 |
|----|----|----|----------|
| 周一 | 无新课 | **阶段总验收 ①**：关掉视频和仓库，从空文件开始搭数据管线 + embedding 层（30 分钟，第 1 段） | 数据加载与 embedding 前向跑通，存盘留待明天继续 |
| 周二 | 无新课 | **阶段总验收 ②**：接着写隐藏层 + 前向 + loss（30 分钟，第 2 段） | 前向输出维度正确，loss 可计算 |
| 周三 | 无新课 | **阶段总验收 ③**：接着写训练循环 + 采样函数（30 分钟，第 3 段） | 训练循环完整，能采样出"人名" |
| 周四 | 无新课 | **阶段总验收 ④**：跑通完整训练并检查结果（30 分钟，第 4 段） | 独立写完并能训练（原限时 2 小时拆为周一~周四 4 个 session 连续完成） |
| 周五 | 无新课 | 关掉代码，口头（或对自己录音）讲清反向传播的每一步 | 讲得顺；讲不清的回炉对应那一周 |
| 周六 | 缓冲 / 机动 | 补齐总验收未完成的部分 | — |
| 周日 | 休整 | 阶段二总结写入 `notes/`：micrograd/makemore 各 3 句话 | 讲不清的回炉对应那一周 |

## 第 24 周结束你应该具备

- [ ] 能手写自动微分引擎并解释拓扑排序反向传播
- [ ] 能手推常见层的梯度（这是阶段三读论文和手写 attention 的前置）
- [ ] 理解语言模型 = 下一个 token 预测，embedding/NLL/采样全链路
- [ ] 可以无缝衔接阶段三（第 25 周起）：手写 Transformer/GPT（`docs/阶段三每日计划.md`）
